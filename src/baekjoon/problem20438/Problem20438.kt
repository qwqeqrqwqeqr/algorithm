package baekjoon.problem20438

fun main(args: Array<String>) = with(System.`in`.bufferedReader()) {
    val (N,K,Q,M) = readLine().split(" ").map { it.toInt() }
    val kList = readLine().split(" ").map { it.toInt() }
    val qList = readLine().split(" ").map { it.toInt() }
    
}