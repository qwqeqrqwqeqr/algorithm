package programmers.binarysearch.bridge


class Bridge {

    fun solution(distance: Int, rocks: IntArray, n: Int): Int {
        var answer = 0
        rocks.sort()


        return answer
    }
}

fun main() {
    val bridge = Bridge()
    val distance = 25
    val rocks = intArrayOf(7, 10)
    val n =2
    bridge.solution(distance,rocks,n)
}